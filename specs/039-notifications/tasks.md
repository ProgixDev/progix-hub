# Tasks — Spec 039 (notifications)

- [x] Migration 0046: notification_reads (own-row RLS) + unread_notification_count() RPC (invoker)
- [x] activity slice: loadNotificationsAction + markNotificationsReadAction; NotificationsBell
- [x] Thread notificationsSlot through app-shell → app-frame → top-bar
- [x] Mount NotificationsBell on all 11 AppShell pages; i18n notifications (en+fr)
- [x] Verify: badge count, dropdown items, mark-read on open
- [ ] appsec + ship
