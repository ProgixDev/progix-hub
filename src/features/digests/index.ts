// Public API for the digests slice. data fns are SERVER-ONLY (data.ts).
export { getLatestDigest } from "./data";
export { DigestSection } from "./components/digest-section";
export type { ProjectDigest } from "./types";
