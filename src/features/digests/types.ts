/** A stored AI-generated weekly digest for a project (spec 038). */
export type ProjectDigest = {
  id: string;
  project_id: string;
  content_md: string;
  period_start: string;
  period_end: string;
  model: string;
  created_at: string;
};
