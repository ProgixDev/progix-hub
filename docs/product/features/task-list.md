# Task list (demo feature) — removed at project init

The skeleton’s demo `task-list` slice was removed when this clone was initialized as **progixHub** (2026-06-08). Its spec (`specs/001-task-list`) is marked abandoned. progixHub’s first real feature is **project creation + the four links** — see [../prd.md](../prd.md).
